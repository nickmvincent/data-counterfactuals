Bibliography and reading-list data now live in Semble.

This directory remains only as a stable path placeholder for tooling and docs.

To build the site, configure one of:

- `SEMBLE_PROFILE_IDENTIFIER`
- `SEMBLE_COLLECTION_AT_URIS`

Successful loads also write `tmp/semble-cache.json` for offline dev and local analysis.
